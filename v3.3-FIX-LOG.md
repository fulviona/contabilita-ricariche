# v3.3 FIX LOG — 2026-03-06 13:40

- Data obbligatoria per **Ricarica** e **Acconto** (campo `date` con default = oggi).
- Aggiunto array `movimenti[]` per ogni cliente: { tipo: 'ricarica'|'acconto', importo, data } (solo per Storico).
- Storico movimenti nel Dettaglio cliente (ordine: più recenti in alto).
- Clic su "Ricariche" (titolo) → ritorna alla Home.
- Dati sempre locali (IndexedDB), offline-first.
